{
  "name": "stream-rating-overlay",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "Tests only. The extension itself ships as plain JS/CSS with no build step — nothing here is bundled or required at runtime.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
